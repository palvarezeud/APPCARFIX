# Sesion de Trabajo — 2026-06-26

Proyecto: CAR_FIX — Sistema de Reparacion de Vehiculos  
Participantes: Usuario (Pablo Alvarez) + Claude Code (claude-sonnet-4-6)

---

## 1. Actualizacion del Esquema de Base de Datos

### Accion
Se ejecuto el skill `/sync-db-docs` para regenerar `DATABASE_DOCUMENTATION.md` con los cambios que el usuario habia realizado directamente en SQL Server.

### Cambios detectados en la BD
El esquema cambio significativamente respecto a la version anterior:

**`OrdenReparacion`**
- Eliminado: columna `Total` (el total ahora vive en `Facturas`)
- Renombrado: `Garantia` → `EsGarantia` (consistente con `EsEmpresa`, `EsAutomatico`)
- Agregado: `FacturaID int NOT NULL` (FK hacia `Facturas`)

**`Reparacion`**
- Cambiado: `OrdenReparacionID` → `FacturaID` (ahora FK con CASCADE hacia `Facturas`)

**`Repuesto`**
- Cambiado: `OrdenReparacionID` → `FacturaID` (ahora FK con CASCADE hacia `Facturas`)

**`Facturas`**
- Agregado: `OrdenReparacionID int NOT NULL` (FK logica)
- Agregado: `Adelanto money NOT NULL DEFAULT ((0))`
- Agregado: `ImpuestoVentas money NOT NULL DEFAULT ((0))`
- Agregado: `EsProforma bit NOT NULL DEFAULT ((0))`
- Eliminado: `RepuestoID` y `ReparacionID`
- `ClienteID` y `EstadoFacturaID` ahora tienen FK constraint enforced

**`TipoReparacion`**
- Renombrado: `DuracionAproximada` → `DuracionAproximadaHoras`

**Nueva tabla `Taller`** (ya estaba en la sesion anterior, se confirmo)

### Cambio arquitectonico
`Reparacion` y `Repuesto` ahora enlazan directamente a `Facturas` (no a `OrdenReparacion`). Existe referencia circular: `Facturas.OrdenReparacionID` ↔ `OrdenReparacion.FacturaID`.

### Archivos modificados
- `.claude/DATABASE_DOCUMENTATION.md` — secciones ERD y Tablas regeneradas

---

## 2. Guia de Buenas Practicas Angular 21

### Accion
El usuario tenia un archivo `Angular_Buenas_Practicas.md` en la raiz del proyecto. Se analizo su contenido, se limpio (tenia artefactos de formato) y se ubico correctamente.

### Decisiones
- Archivo limpio guardado en: `.claude/angular_buenas_practicas.md`
- Archivo original eliminado de la raiz
- Importado en `CLAUDE.md` con `@.claude/angular_buenas_practicas.md` para carga automatica

### Contenido de la guia (resumen)
Patrones obligatorios para Angular 21:
- Componentes `standalone: true` (no NgModule)
- Signals: `signal()`, `computed()`, `effect()`
- Control de flujo: `@if`, `@for`, `@switch` (no `*ngIf`, `*ngFor`)
- Inputs/Outputs: `input()`, `output()`, `model()` (no decoradores `@Input/@Output`)
- Inyeccion de dependencias: `inject()` (no constructor injection)
- Configuracion: `provideRouter()`, `provideHttpClient()` (no modulos)
- Lazy loading: `loadComponent()` por componente standalone
- `@defer` para carga diferida en plantillas

---

## 3. Guia de Arquitectura Limpia — Backend ASP.NET Core

### Stack confirmado por el usuario
- **Framework:** ASP.NET Core Web API
- **Version:** .NET 10
- **Lenguaje:** C#
- **ORM:** Entity Framework Core — **Database First**
- **Autenticacion:** JSON Web Token (JWT)
- **Pruebas:** Spec Driven Development con Reqnroll

### Archivo creado
`.claude/arquitectura_limpia_backend.md`

### Decisiones de arquitectura tomadas en esta sesion

#### Estructura de la solucion (5 proyectos en 5 carpetas fisicas)
```
Backend/
├── CarFix.sln
├── Dominio/           → CarFix.Dominio
├── Aplicacion/        → CarFix.Aplicacion
├── Infraestructura/   → CarFix.Infraestructura
├── WebApi/            → CarFix.WebApi
└── Especificaciones/  → CarFix.Especificaciones
```

#### Patrones incluidos
- **Clean Architecture**: dependencias unidireccionales hacia adentro
- **CQRS con MediatR**: Commands (escritura) / Queries (lectura directa al DbContext)
- **Patron Resultado**: `Resultado<T>` con `EsExitoso`, `Valor`, `Error`, `Exito()`, `Fallo()`
- **Unidad de Trabajo**: `IUnidadTrabajo` / `UnidadTrabajo` con `GuardarCambiosAsync()`
- **Inversion de Control**: capas internas definen interfaces, Infrastructure implementa
- **Inyeccion de Dependencias**: cada capa tiene `InjeccionDependencias.cs`
- **Repositorios especificos**: metodos con nombres de dominio en espannol
- **Validacion automatica**: `ComportamientoValidacion` pipeline de MediatR + FluentValidation

#### Entity Framework Database First
- Entidades generadas con `dotnet ef dbcontext scaffold`
- Archivos generados en `Infraestructura/Persistencia/Generado/` — nunca editar
- Comportamiento de dominio agregado via clases parciales en `Infraestructura/Persistencia/Extensiones/`
- Nombres de entidades y propiedades identicos a los de la BD

#### JWT
- Paquete: `Microsoft.AspNetCore.Authentication.JwtBearer`
- `ServicioToken` en `Infraestructura/Seguridad/`
- Todos los endpoints protegidos con `.RequireAuthorization()`
- Unico endpoint publico: `POST /api/autenticacion/iniciar-sesion`
- Llave secreta nunca en repositorio — usar `dotnet user-secrets`

#### .NET 10 — APIs especificas
- `TypedResults` (no `Results`) para inferencia de tipos en OpenAPI
- `IExceptionHandler` + `AddExceptionHandler<ManejadorExcepciones>()`
- `AddOpenApi()` + `MapOpenApi()` — sin Swashbuckle
- `AddProblemDetails()` nativo

#### Spec Driven Development con Reqnroll
- Proyecto `Especificaciones/CarFix.Especificaciones`
- Herramienta: Reqnroll (reemplazo activo de SpecFlow para .NET 10)
- Specs cubren la capa de Aplicacion (handlers de MediatR)
- BD en memoria (`UseInMemoryDatabase`) — cada escenario parte limpio
- Formato Gherkin: `Feature` / `Background` / `Scenario` / `Scenario Outline`
- Flujo: escribir `.feature` → implementar steps → implementar handler → spec pasa

---

## 4. Convenciones de Nomenclatura

### Decision
Todo el codigo se escribe en **espannol sin caracteres especiales**.

### Reglas
- Sin tildes: `numero`, `pagina`, `descripcion`
- Sin nn: `annio` (año), `espannol` (español)
- Nombres de entidades/propiedades identicos a los de las tablas/columnas de la BD
- Sufijos tecnicos en ingles permitidos: `Handler`, `Repository`, `Validator`, `Command`, `Query`, `Dto`, `Exception`
- Metodos de dominio en espannol: `ObtenerPorIdAsync`, `AgregarAsync`, `Actualizar`, `GuardarCambiosAsync`
- Archivos de DI: `InjeccionDependencias.cs`
- Metodos de extension DI: `AgregarAplicacion()`, `AgregarInfraestructura()`

---

## 5. Organizacion de Archivos del Proyecto

### Decision
- `CLAUDE.md` debe quedarse en la raiz del repositorio (convencion de Claude Code, no se puede mover)
- `PENDIENTES.md` y `DATABASE_DOCUMENTATION.md` movidos a `.claude/`
- Todas las referencias en `CLAUDE.md` y el skill `sync-db-docs` actualizadas

### Estructura final de `.claude/`
```
.claude/
├── settings.json
├── PENDIENTES.md
├── DATABASE_DOCUMENTATION.md
├── angular_buenas_practicas.md
├── arquitectura_limpia_backend.md
└── skills/
    └── sync-db-docs/
        └── SKILL.md
```

---

## 6. Estructura General del Repositorio (definida)

```
AppCarFix/
├── CLAUDE.md
├── .claude/
│   └── (archivos de soporte Claude)
├── Backend/
│   ├── CarFix.sln
│   ├── Dominio/
│   ├── Aplicacion/
│   ├── Infraestructura/
│   ├── WebApi/
│   └── Especificaciones/
├── Frontend/
│   └── car-fix-app/     (ng new car-fix-app)
└── PROMPTS/
    └── sesion_2026-06-26.md
```

---

## 7. Pendientes al Cierre de la Sesion

Ver `.claude/PENDIENTES.md` para la lista completa. Items principales:
- Crear tabla `Usuarios` y tabla `Roles` en la BD
- Agregar FK constraint `Reparacion.TipoReparacionID` → `TipoReparacion.CatalogoCostoID`
- Corregir typo `EstadoFactura.Descipcion` → `Descripcion`
- Evaluar `Vehiculos.Annio` de `tinyint` a `smallint`
- Renombrar `TipoReparacion.CatalogoCostoID` → `TipoReparacionID`
- Agregar campo `Cantidad` en `Repuesto`

---

## 8. Proximos Pasos de Desarrollo

1. Crear la solucion .NET en `Backend/` con los 5 proyectos
2. Ejecutar el scaffold de EF Core desde la BD existente
3. Crear el proyecto Angular en `Frontend/` con `ng new car-fix-app`
4. Implementar autenticacion JWT (requiere tablas `Usuarios` y `Roles` primero)
5. Comenzar por los casos de uso prioritarios siguiendo el flujo de negocio:
   - Registro de clientes y vehiculos
   - Creacion de ordenes de reparacion
   - Asociacion de reparaciones y repuestos
   - Generacion de facturas
